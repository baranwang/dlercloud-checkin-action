{
  "name": "dlercloud-checkin-action",
  "version": "1.3.0",
  "description": "DlerCloud Automatic Checkin",
  "main": "lib/main.js",
  "scripts": {
    "build": "tsc && ncc build",
    "start": "pnpm run build && node dist",
    "prepare": "husky install"
  },
  "author": "baranwang",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^17.0.13",
    "@vercel/ncc": "^0.33.1",
    "husky": "^7.0.0",
    "typescript": "^4.5.5"
  },
  "dependencies": {
    "@actions/core": "^1.6.0",
    "node-fetch": "^3.2.0"
  }
}
